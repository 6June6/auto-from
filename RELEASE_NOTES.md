# 🎉 发布说明

## 📦 打包完成

✅ **macOS 版本已成功打包！**

- 📁 位置: `dist/自动表单填写工具.app`
- 📏 大小: 约 415 MB
- 🎯 支持: macOS 10.13+ (Intel & Apple Silicon)

## 📋 打包文件说明

### macOS 打包结果

```
dist/
├── 自动表单填写工具.app      # macOS 应用程序（双击运行）
├── 自动表单填写工具/          # 目录版本（包含所有文件）
└── README.txt               # 使用说明
```

### 文件大小分析

| 文件      | 大小   | 说明                                  |
| --------- | ------ | ------------------------------------- |
| 总计      | ~415MB | 包含完整的 Python + PyQt6 + WebEngine |
| 主程序    | ~10MB  | 应用逻辑代码                          |
| PyQt6     | ~200MB | GUI 框架                              |
| WebEngine | ~150MB | 浏览器引擎                            |
| 其他依赖  | ~55MB  | Python 运行时和其他库                 |

## 🚀 分发方式

### macOS 分发

#### 方式 1：直接分发（推荐）

```bash
# 1. 压缩应用程序
cd dist
zip -r 自动表单填写工具_macOS_v1.0.zip 自动表单填写工具.app

# 2. 分发 ZIP 文件
# 用户下载后解压，双击运行
```

#### 方式 2：DMG 安装包

```bash
# 安装 create-dmg
brew install create-dmg

# 创建 DMG
create-dmg \
  --volname "自动表单填写工具" \
  --window-pos 200 120 \
  --window-size 800 400 \
  --icon-size 100 \
  --icon "自动表单填写工具.app" 200 190 \
  --hide-extension "自动表单填写工具.app" \
  --app-drop-link 600 185 \
  "自动表单填写工具_v1.0.dmg" \
  "dist/自动表单填写工具.app"
```

### Windows 分发

#### 方式 1：直接分发

```cmd
# 在 Windows 上打包后
cd dist
# 压缩 "自动表单填写工具" 文件夹为 ZIP
# 分发 ZIP 文件
```

#### 方式 2：Inno Setup 安装包

1. 下载安装 [Inno Setup](https://jrsoftware.org/isdl.php)
2. 使用提供的脚本创建安装程序
3. 生成 .exe 安装包

## 📝 Windows 打包步骤

### 在 Windows 电脑上执行：

```cmd
# 1. 克隆或复制项目到 Windows
cd C:\path\to\auto-form-filler-py

# 2. 创建虚拟环境
python -m venv venv
venv\Scripts\activate

# 3. 安装依赖
pip install -r requirements.txt
pip install pyinstaller

# 4. 运行打包脚本
build_windows.bat

# 或手动打包
pyinstaller --name="自动表单填写工具" --windowed --onedir --clean main.py
```

## ⚠️ 首次运行提示

### macOS

用户首次运行可能遇到：

**问题**: "无法打开，因为它来自身份不明的开发者"

**解决方案**:

1. 右键点击应用 > 选择"打开"
2. 在弹窗中点击"打开"
3. 或在系统偏好设置 > 安全性与隐私 > 点击"仍要打开"

### Windows

**问题**: Windows Defender SmartScreen 提示

**解决方案**:

1. 点击"更多信息"
2. 点击"仍要运行"

## 🔒 代码签名（可选）

### macOS 签名（需要 Apple Developer 账号 $99/年）

```bash
# 签名应用
codesign --deep --force --verify --verbose \
  --sign "Developer ID Application: Your Name (TEAM_ID)" \
  dist/自动表单填写工具.app

# 公证应用（需要上传到 Apple 服务器验证）
xcrun notarytool submit \
  dist/自动表单填写工具.zip \
  --apple-id "your@email.com" \
  --password "app-specific-password" \
  --team-id "TEAM_ID" \
  --wait

# 附加公证票据
xcrun stapler staple dist/自动表单填写工具.app
```

### Windows 签名（需要购买代码签名证书）

```cmd
# 使用 SignTool 签名
signtool sign /f certificate.pfx /p password /t http://timestamp.digicert.com dist\自动表单填写工具\自动表单填写工具.exe
```

## 📊 版本信息

- **版本**: 1.0.0
- **打包日期**: 2025-10-09
- **Python**: 3.9.6
- **PyQt6**: 6.9.1
- **PyInstaller**: 6.16.0

## 🎯 功能特性

✅ 智能表单填写
✅ 名片管理
✅ 链接管理  
✅ 页面诊断
✅ 麦客 CRM 特殊支持
✅ 填写记录追踪
✅ 现代化 GUI 界面

## 📞 技术支持

- **问题反馈**: 提交 GitHub Issue
- **使用文档**: 查看 README.md
- **开发文档**: 查看 BUILD_GUIDE.md

## 🔄 更新计划

- [ ] 添加应用图标
- [ ] 支持更多表单类型
- [ ] 批量填写功能
- [ ] 数据导入导出
- [ ] 自动更新功能

---

**Happy Coding! 🚀**
