# 表单匹配流程总结

## 📋 目录

- [共享组件](#共享组件)
- [腾讯文档流程](#腾讯文档流程)
- [WPS流程](#wps流程)
- [腾讯问卷流程](#腾讯问卷流程)
- [石墨文档流程](#石墨文档流程)
- [金数据流程](#金数据流程)
- [飞书问卷流程](#飞书问卷流程)
- [问卷网流程](#问卷网流程)
- [问卷星流程](#问卷星流程)
- [报名工具流程](#报名工具流程)
- [番茄表单流程](#番茄表单流程)
- [见数流程](#见数流程)
- [麦客表单流程](#麦客表单流程)
- [流程对比](#流程对比)

---

## 🔧 共享组件

### 位置

`core/tencent_docs_filler.py`

### 1. 共享匹配算法

**方法**: `TencentDocsFiller.get_shared_match_algorithm()`

**包含函数**:

```javascript
cleanText(text); // 清理文本（转小写，去除标点符号）
splitKeywords(keyword); // 分割关键词（支持 |,;，；、/ 等分隔符）
matchKeyword(identifiers, keyword); // 匹配评分系统
```

**评分规则**:

- 100分：完全匹配（`cleanIdentifier === cleanKeyword`）
- 80-90分：包含匹配（`cleanIdentifier.includes(cleanKeyword)`）
- 70分：反向包含（`cleanKeyword.includes(cleanIdentifier)`）
- 30-60分：字符相似度匹配（相似度≥50%）
- **阈值**: 50分（低于50分视为不匹配）

### 2. 共享执行逻辑

**方法**: `TencentDocsFiller.get_shared_execution_logic()`

**核心函数**: `createSharedExecutor(config)`

**配置参数**:

```javascript
{
    fillData: [],          // 名片数据数组 [{ key, value }, ...]
    allInputs: [],         // 所有输入框元素数组
    getIdentifiers: fn,    // 提取标识符的函数 (input, index) => [identifiers]
    fillInput: fn,         // 填充函数 (input, value) => {}
    onProgress: fn         // 进度回调（可选）
}
```

**执行流程**:

1. 扫描页面输入框
2. 逐个遍历输入框
3. **对每个输入框独立查找最高分的名片数据**
4. 名片数据可以被多次使用（不会被"消耗"）
5. 只接受分数≥50的匹配
6. 执行填充（延迟50ms）

---

## 📝 腾讯文档流程

### 文件位置

`core/tencent_docs_filler.py::generate_fill_script()`

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. Python 层：准备数据                      │
│    - field_data: {key: value}              │
│    - 转换为 JavaScript 对象                 │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 注入共享算法                             │
│    shared_algorithm =                       │
│      get_shared_match_algorithm()           │
│    shared_executor =                        │
│      get_shared_execution_logic()           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. JavaScript 层：等待页面加载              │
│    waitForPageReady()                       │
│    - 查找 .question[data-qid]               │
│    - 最多尝试 10 次，每次等待 500ms         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. 获取所有问题元素                         │
│    questions = document.querySelectorAll(   │
│      '.question[data-qid]'                  │
│    )                                        │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. 调用共享执行器                           │
│    const executor = createSharedExecutor({  │
│      fillData: 转换为数组格式,              │
│      allInputs: questions,                  │
│      getIdentifiers: 提取问题标题,          │
│      fillInput: fillInputTencent            │
│    })                                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. 共享执行器内部流程                       │
│    ├─ 扫描所有 questions                    │
│    ├─ 逐个遍历                              │
│    ├─ 对每个 question:                      │
│    │   ├─ 提取标题 (getIdentifiers)         │
│    │   ├─ 遍历所有名片数据                  │
│    │   ├─ 调用 matchKeyword() 评分          │
│    │   ├─ 选择最高分（≥50）                 │
│    │   └─ 调用 fillInput() 填充             │
│    └─ 汇总结果                              │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. 腾讯文档专用填充逻辑                     │
│    fillInputTencent(questionElement, value) │
│    ├─ 找到 textarea 输入框                  │
│    ├─ 移除 disabled 属性                    │
│    ├─ 移除 readonly 属性                    │
│    ├─ 聚焦输入框                            │
│    ├─ 设置值: input.value = value           │
│    ├─ 触发事件: input, change, blur         │
│    └─ 失焦: input.blur()                    │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 8. 返回结果                                 │
│    window.__autoFillResult__ = {            │
│      status: 'success',                     │
│      message: '...',                        │
│      filled: [...],                         │
│      failed: [...]                          │
│    }                                        │
└─────────────────────────────────────────────┘
```

### 关键特点

- ✅ DOM 结构: `.question[data-qid]` 问题元素
- ✅ 输入框: `textarea` 元素
- ✅ 标识符: 问题标题文本（`.question-title .form-auto-ellipsis`）
- ✅ 需要处理 `disabled` 和 `readonly` 属性

---

## 🎯 WPS流程

### 文件位置

`gui/new_fill_window.py::generate_kdocs_fill_script()`

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. Python 层：准备数据                      │
│    - fill_data: [{ key, value }, ...]      │
│    - 已经是数组格式                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 注入共享算法                             │
│    shared_algorithm =                       │
│      get_shared_match_algorithm()           │
│    shared_executor =                        │
│      get_shared_execution_logic()           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. 适配移动端视口                           │
│    adaptViewport()                          │
│    - 修改 viewport meta 标签                │
│    - 注入响应式 CSS                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. JavaScript 层：等待输入框加载            │
│    waitForInputs()                          │
│    - 查找 input, textarea                   │
│    - 最多尝试 15 次，每次等待 400ms         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. 获取所有输入框（去重优化）               │
│    getAllInputs()                           │
│    ├─ 查找所有 input, textarea              │
│    ├─ 过滤不可见元素                        │
│    └─ Radio/Checkbox 去重:                  │
│        ├─ 方式1: 使用 name 属性             │
│        ├─ 方式2: 使用容器 ID/className      │
│        └─ 方式3: 使用问题标题               │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. 调用共享执行器                           │
│    const executor = createSharedExecutor({  │
│      fillData: fillData,                    │
│      allInputs: getAllInputs(),             │
│      getIdentifiers: getInputIdentifiers,   │
│      fillInput: fillInput                   │
│    })                                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. 共享执行器内部流程                       │
│    ├─ 扫描所有 inputs                       │
│    ├─ 逐个遍历                              │
│    ├─ 对每个 input:                         │
│    │   ├─ 提取标识符 (getInputIdentifiers)  │
│    │   ├─ 遍历所有名片数据                  │
│    │   ├─ 调用 matchKeyword() 评分          │
│    │   ├─ 选择最高分（≥50）                 │
│    │   └─ 调用 fillInput() 填充             │
│    └─ 汇总结果                              │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 8. WPS 专用标识符提取                       │
│    getInputIdentifiers(input, index)        │
│    ├─ 方法1: 查找容器标题元素               │
│    │   .ksapc-question-title-title          │
│    ├─ 方法2: 查找序号+标题                  │
│    ├─ 方法3: aria-labelledby (非radio)      │
│    ├─ 方法4: Label 标签关联 (非radio)       │
│    ├─ 方法5: placeholder (非radio)          │
│    ├─ 方法6: 前置兄弟元素 (非radio)         │
│    └─ 最终过滤:                             │
│        - Radio/Checkbox: 移除选项文本       │
│          (图文、视频、是、否、50% 等)       │
│        - 按优先级排序                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 9. WPS 专用填充逻辑                         │
│    fillInput(input, value)                  │
│    ├─ 移除 disabled 属性                    │
│    ├─ 移除 readonly 属性                    │
│    ├─ 聚焦输入框: input.focus()             │
│    ├─ 延迟 50ms                             │
│    ├─ 设置值:                               │
│    │   - text/textarea: input.value = value │
│    │   - radio/checkbox: 点击匹配的选项     │
│    ├─ 触发事件: input, change               │
│    ├─ 触发 React 事件（如果存在）           │
│    └─ 失焦: input.blur()                    │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 10. 返回结果                                │
│     window.__autoFillResult__ = {           │
│       fillCount: number,                    │
│       totalCount: number,                   │
│       status: 'completed',                  │
│       results: [...]                        │
│     }                                       │
└─────────────────────────────────────────────┘
```

### 关键特点

- ✅ DOM 结构: 通用 `input, textarea` 元素
- ✅ 容器: `.ksapc-questions-write-container`
- ✅ 标识符提取复杂（6种方法）
- ✅ Radio/Checkbox 特殊处理（去重 + 过滤选项文本）
- ✅ 支持 React 表单（触发 React 内部事件）

---

## 🐧 腾讯问卷流程

### 文件位置
`gui/new_fill_window.py::generate_tencent_wj_fill_script()`

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. Python 层：准备数据                      │
│    - fill_data: [{ key, value }, ...]      │
│    - 已经是数组格式                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 注入共享算法                             │
│    shared_algorithm =                       │
│      get_shared_match_algorithm()           │
│    shared_executor =                        │
│      get_shared_execution_logic()           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. 适配移动端视口                           │
│    adaptViewport()                          │
│    - 修改 viewport meta 标签                │
│    - 注入响应式 CSS                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. JavaScript 层：等待问卷加载              │
│    waitForForm()                            │
│    - 查找 .question                         │
│    - 最多尝试 20 次，每次等待 400ms         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. 获取所有字段                             │
│    getAllFields()                           │
│    ├─ 方式1: 查找标准结构 .question         │
│    │   - 标题: .question-title .text .pe-line │
│    │   - 输入: .inputs-input, input, textarea │
│    └─ 方式2: 通用查找 + 问题标签配对        │
│        - 按索引与 getQuestionLabels() 配对  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. 调用共享执行器                           │
│    const executor = createSharedExecutor({  │
│      fillData: fillData,                    │
│      allInputs: allFields,                  │
│      getIdentifiers: (field) => [field.title], │
│      fillInput: (field, value) =>           │
│        fillInput(field.input, value)        │
│    })                                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. 共享执行器内部流程                       │
│    ├─ 扫描所有 fields                       │
│    ├─ 逐个遍历                              │
│    ├─ 对每个 field:                         │
│    │   ├─ 提取标题 (getIdentifiers)         │
│    │   ├─ 遍历所有名片数据                  │
│    │   ├─ 调用 matchKeyword() 评分          │
│    │   ├─ 选择最高分（≥50）                 │
│    │   └─ 调用 fillInput() 填充             │
│    └─ 汇总结果                              │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 8. 腾讯问卷专用填充逻辑                     │
│    fillInput(input, value)                  │
│    ├─ 聚焦输入框: focus() + click()         │
│    ├─ 清空内容: input.value = ''            │
│    ├─ 使用原生 setter 设置值（React关键）   │
│    ├─ 触发 InputEvent (insertText)          │
│    ├─ 触发 change 事件                      │
│    ├─ 模拟键盘事件序列                      │
│    ├─ 再次确认值已设置                      │
│    ├─ 触发 blur 事件                        │
│    ├─ 尝试触发 React 内部状态更新           │
│    └─ Vue 兼容: __vue__.$emit('input')      │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 9. 返回结果                                 │
│    window.__autoFillResult__ = {            │
│      fillCount: number,                     │
│      totalCount: number,                    │
│      status: 'completed',                   │
│      results: [...]                         │
│    }                                        │
└─────────────────────────────────────────────┘
```

### 关键特点
- ✅ DOM 结构: `.question` 问题容器
- ✅ 标题选择器: `.question-title .text .pe-line`
- ✅ 输入框: `.inputs-input, input, textarea`
- ✅ 填充兼容 React/Vue 框架
- ✅ 特殊处理：`getQuestionLabels()` 从页面文本提取标签

---

## 📄 石墨文档流程

### 文件位置
`gui/new_fill_window.py::generate_shimo_fill_script()`

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. Python 层：准备数据                      │
│    - fill_data: [{ key, value }, ...]      │
│    - 已经是数组格式                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 注入共享算法                             │
│    shared_algorithm =                       │
│      get_shared_match_algorithm()           │
│    shared_executor =                        │
│      get_shared_execution_logic()           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. 适配移动端视口                           │
│    adaptViewport()                          │
│    - 修改 viewport meta 标签                │
│    - 注入响应式 CSS                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. JavaScript 层：等待输入框加载            │
│    waitForInputs()                          │
│    - 查找 input, textarea                   │
│    - 最多尝试 15 次，每次等待 400ms         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. 获取所有输入框（带过滤）                 │
│    getAllInputs()                           │
│    ├─ 查找所有 input, textarea              │
│    ├─ 排除举报/投诉相关输入框               │
│    ├─ 排除模态框/弹窗中的输入框             │
│    ├─ 排除搜索框                            │
│    └─ 确保输入框可见（有尺寸）              │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. 调用共享执行器                           │
│    const executor = createSharedExecutor({  │
│      fillData: fillData,                    │
│      allInputs: allInputs,                  │
│      getIdentifiers: getInputIdentifiers,   │
│      fillInput: fillInput                   │
│    })                                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. 石墨文档专用标识符提取                   │
│    getInputIdentifiers(input, index)        │
│    ├─ 方法1: fieldset容器精确匹配           │
│    │   .Title-sc-ci5sac                     │
│    ├─ 方法2: QuestionTitle 通用匹配         │
│    ├─ 方法3: 向上查找标题容器               │
│    ├─ 方法4: aria-labelledby 属性           │
│    ├─ 方法5: Label 标签关联                 │
│    ├─ 方法6: placeholder, title, aria-label │
│    └─ 方法7: 前置兄弟元素                   │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 8. 石墨文档专用填充逻辑                     │
│    fillInput(input, value)                  │
│    ├─ 检测数字输入框（特殊处理）            │
│    ├─ 重置 React valueTracker               │
│    ├─ 使用原生 setter 设置值                │
│    ├─ 触发 InputEvent (insertText)          │
│    ├─ 触发 change 事件                      │
│    ├─ 调用 React onChange 处理器            │
│    ├─ 触发 CompositionEvent（中文输入）     │
│    ├─ 模拟键盘事件                          │
│    ├─ 数字输入框: 强制更新 Fiber 状态       │
│    └─ 延迟触发 blur                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 9. 返回结果                                 │
│    window.__autoFillResult__ = {            │
│      fillCount: number,                     │
│      totalCount: number,                    │
│      status: 'completed',                   │
│      results: [...]                         │
│    }                                        │
└─────────────────────────────────────────────┘
```

### 关键特点
- ✅ DOM 结构: `fieldset` 问题容器
- ✅ 标题选择器: `.Title-sc-ci5sac`、`.QuestionTitle-sc-*`
- ✅ 输入框: `input, textarea`
- ✅ 特殊处理：数字输入框需要额外的 React Fiber 更新
- ✅ 填充兼容 React 18+ 框架

---

## 📊 金数据流程

### 文件位置
`gui/new_fill_window.py::generate_jinshuju_fill_script()`

### 域名
- `jsj.top` - 主域名
- `jinshuju.net` - 备用域名

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. Python 层：准备数据                      │
│    - fill_data: [{ key, value }, ...]      │
│    - 已经是数组格式                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 注入共享算法                             │
│    shared_algorithm =                       │
│      get_shared_match_algorithm()           │
│    shared_executor =                        │
│      get_shared_execution_logic()           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. JavaScript 层：等待输入框加载            │
│    waitForInputs()                          │
│    - 查找 input, textarea                   │
│    - 最多尝试 15 次，每次等待 400ms         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. 获取所有输入框                           │
│    getAllInputs()                           │
│    ├─ 查找所有 input, textarea              │
│    └─ 过滤不可见元素                        │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. 调用共享执行器                           │
│    const executor = createSharedExecutor({  │
│      fillData: fillData,                    │
│      allInputs: allInputs,                  │
│      getIdentifiers: getInputIdentifiers,   │
│      fillInput: fillInput                   │
│    })                                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. 金数据专用标识符提取                     │
│    getInputIdentifiers(input)               │
│    ├─ 方法1: 金数据专用容器                 │
│    │   .field-container, [data-api-code]    │
│    │   → .ant-form-item-label .label-item   │
│    ├─ 方法2: 向上查找 ant-form-item 容器    │
│    └─ 方法3: 基本属性 (name, id, title)     │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. 金数据专用填充逻辑                       │
│    fillInput(input, value)                  │
│    ├─ 聚焦输入框: focus() + click()         │
│    ├─ 清空内容: input.value = ''            │
│    ├─ 使用原生 setter 设置值                │
│    ├─ 触发 InputEvent (insertText)          │
│    ├─ 触发 change 事件                      │
│    ├─ 模拟键盘事件序列                      │
│    ├─ 确认值已设置                          │
│    ├─ 触发 blur 事件                        │
│    ├─ 尝试触发 React/Ant Design 更新        │
│    └─ Ant Design: CompositionEvent          │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 8. 返回结果                                 │
│    window.__autoFillResult__ = {            │
│      fillCount: number,                     │
│      totalCount: number,                    │
│      status: 'completed',                   │
│      results: [...]                         │
│    }                                        │
└─────────────────────────────────────────────┘
```

### 关键特点
- ✅ 域名：`jsj.top` 和 `jinshuju.net`
- ✅ DOM 结构: `.field-container`, `[data-api-code]`
- ✅ 标题选择器: `.ant-form-item-label .label-item`
- ✅ 输入框: `input, textarea`
- ✅ 填充兼容 React/Ant Design 框架

---

## 🐦 飞书问卷流程

### 文件位置
`gui/new_fill_window.py::generate_feishu_fill_script()`

### 域名
- `feishu.cn`

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. Python 层：准备数据                      │
│    - fill_data: [{ key, value }, ...]      │
│    - 已经是数组格式                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 注入共享算法                             │
│    shared_algorithm =                       │
│      get_shared_match_algorithm()           │
│    shared_executor =                        │
│      get_shared_execution_logic()           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. JavaScript 层：检测表单版本              │
│    detectFormVersion()                      │
│    ├─ 新版/移动端: .bitable-form-item       │
│    └─ 旧版: .base-form-container_card_item  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. 等待表单加载                             │
│    waitForForm()                            │
│    - 最多尝试 25 次，每次等待 400ms         │
│    - 返回 { found, version }                │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. 获取所有字段                             │
│    getAllFields(version)                    │
│    ├─ 新版: getAllFieldsNewVersion()        │
│    │   - .bitable-form-item[data-index]     │
│    │   - 返回 {title, editor, selector,...} │
│    └─ 旧版: getAllFieldsOldVersion()        │
│        - .base-form-container_card_item     │
│        - 返回 {title, editor, ...}          │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. 调用共享执行器                           │
│    const executor = createSharedExecutor({  │
│      fillData: fillData,                    │
│      allInputs: allFields,                  │
│      getIdentifiers: (field) => [field.title]│
│      fillInput: fillInputFeishu             │
│    })                                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. 飞书专用填充逻辑                         │
│    fillInputFeishu(field, value)            │
│    ├─ 选择器类型 → fillSelector()           │
│    │   ├─ 桌面端: .b-select-dropdown-menu   │
│    │   └─ 移动端: .bitable-selector-option  │
│    └─ 编辑器类型 → fillEditor()             │
│        - 富文本 [contenteditable="true"]    │
│        - 创建飞书编辑器内容结构             │
│        - 触发 input/change/blur 事件        │
│        - 触发 CompositionEvent              │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 8. 返回结果                                 │
│    window.__autoFillResult__ = {            │
│      fillCount: number,                     │
│      totalCount: number,                    │
│      status: 'completed',                   │
│      results: [...]                         │
│    }                                        │
└─────────────────────────────────────────────┘
```

### 关键特点
- ✅ 支持新旧两种版本的飞书表单
- ✅ 支持移动端和桌面端两种选择器
- ✅ 富文本编辑器填充（contenteditable）
- ✅ 创建飞书专用的 DOM 结构（ace-line）
- ✅ CompositionEvent 事件触发

---

## 📝 问卷网流程

### 文件位置
`gui/new_fill_window.py::generate_wenjuan_fill_script()`

### 域名
- `wenjuan.com`

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. Python 层：准备数据                      │
│    - fill_data: [{ key, value }, ...]      │
│    - 已经是数组格式                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 注入共享算法                             │
│    shared_algorithm =                       │
│      get_shared_match_algorithm()           │
│    shared_executor =                        │
│      get_shared_execution_logic()           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. JavaScript 层：等待输入框加载            │
│    waitForInputs()                          │
│    - 最多尝试 20 次，每次等待 500ms         │
│    - 查找 input[text], textarea, el-input   │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. 获取所有输入框                           │
│    getAllInputs()                           │
│    ├─ input[type="text"]                    │
│    ├─ textarea                              │
│    ├─ .el-input__inner                      │
│    ├─ .el-textarea__inner                   │
│    ├─ .survey-input input                   │
│    └─ [contenteditable="true"]              │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. 调用共享执行器                           │
│    const executor = createSharedExecutor({  │
│      fillData: fillData,                    │
│      allInputs: allInputs,                  │
│      getIdentifiers: getInputIdentifiers,   │
│      fillInput: fillInput                   │
│    })                                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. 问卷网专用标识符提取                     │
│    getInputIdentifiers(input)               │
│    ├─ 方法1: .question-box 容器             │
│    │   → .question-title 内的文本           │
│    ├─ 方法2: 通用容器匹配                   │
│    │   → .survey-question, .el-form-item    │
│    ├─ 方法3-5: aria/label 关联              │
│    ├─ 方法6: placeholder/title              │
│    └─ 方法7: 前置兄弟元素                   │
│    ※ 自动去除序号前缀和必填标记            │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. 问卷网专用填充逻辑                       │
│    fillInput(input, value)                  │
│    ├─ focus() + 设置 value                  │
│    ├─ 触发完整事件链                        │
│    │   (input, change, blur, key*)          │
│    ├─ 使用原生 setter (Vue 兼容)            │
│    ├─ CompositionEvent                      │
│    └─ blur() 完成编辑                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 8. 返回结果                                 │
│    window.__autoFillResult__ = {            │
│      fillCount: number,                     │
│      totalCount: number,                    │
│      status: 'completed',                   │
│      results: [...]                         │
│    }                                        │
└─────────────────────────────────────────────┘
```

### 关键特点
- ✅ DOM 结构: `.question-box`, `.question-title`
- ✅ 支持 Element UI 组件 (el-input, el-textarea)
- ✅ 自动去除序号前缀 (*1., 2., 等)
- ✅ Vue 框架兼容填充
- ✅ 支持 contenteditable 编辑器

---

## ⭐ 问卷星流程

### 文件位置
`gui/new_fill_window.py::generate_wjx_fill_script()`

### 域名
- `wjx.cn` - 主域名
- `wjx.top` - 备用域名

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. Python 层：准备数据                      │
│    - fill_data: [{ key, value }, ...]      │
│    - 已经是数组格式                         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 注入共享算法                             │
│    shared_algorithm =                       │
│      get_shared_match_algorithm()           │
│    - cleanText, splitKeywords, matchKeyword │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. JavaScript 层：解析问卷星表单结构        │
│    parseWjxFields()                         │
│    ├─ 查找 .field[type] 或 div[topic]       │
│    ├─ 提取 topichtml 问题标题               │
│    └─ 返回 [{element, type, topic, title}]  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. 使用共享匹配算法                         │
│    findBestMatch(identifiers)               │
│    └─ 调用共享的 matchKeyword()             │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. 按题型分类填充                           │
│    switch(type) {                           │
│      case '1','2','6': 文本/多行/数字       │
│        → fillInput() + 智能标题提取         │
│      case '3': 单选题                       │
│        → 点击 .jqradio                      │
│      case '4': 多选题                       │
│        → handleCheckbox() + .jqcheck        │
│      case '7': 下拉选择                     │
│        → handleSelect() + select2           │
│      case '9': 联系地址（矩阵表格）         │
│        → handleAddressField() + 子字段      │
│    }                                        │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. 智能标题提取                             │
│    ├─ 去除序号: "1、" "2." "Q1"             │
│    ├─ 提取括号前核心: "(科技、文创...)"     │
│    ├─ 处理过长标题: 取第一个词组            │
│    └─ 去除尾部数字单位: "10万以上"          │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. 兼容模式                                 │
│    fallbackFill()                           │
│    - 当无法识别问卷星结构时触发             │
│    - 直接扫描所有可编辑输入框               │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 8. 返回结果                                 │
│    window.__autoFillResult__ = {            │
│      fillCount: number,                     │
│      totalCount: number,                    │
│      status: 'completed',                   │
│      results: [...]                         │
│    }                                        │
└─────────────────────────────────────────────┘
```

### 关键特点
- ✅ 域名：`wjx.cn` 和 `wjx.top`
- ✅ 完全使用共享匹配算法（matchKeyword）
- ✅ 支持多种题型：文本、单选、多选、下拉、矩阵
- ✅ 智能标题提取（去序号、去说明）
- ✅ 兼容模式降级处理

---

## 📱 报名工具流程

### 文件位置
`core/baoming_tool_filler.py::BaomingToolFiller.match_and_fill()`

### 域名
- `baominggongju.com`
- `p.baominggongju.com`

### 特殊说明
报名工具是唯一一个使用 **Python 端匹配算法** 的平台（其他平台都是 JavaScript 注入）。
这是因为报名工具需要通过 API 提交表单，而不是在网页中直接填充。

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. 扫码登录                                 │
│    - 获取微信登录二维码                     │
│    - 轮询登录状态                           │
│    - 获取 access_token                      │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 获取表单结构                             │
│    api.get_form_fields()                    │
│    - 从 API 获取字段列表                    │
│    - 包含 field_name, field_key, field_type │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. Python 端匹配                            │
│    match_and_fill(card_config)              │
│    └─ 调用 SharedMatchAlgorithm.match_keyword()
│       （来自 tencent_docs_filler.py）       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. 渲染自定义表单页面                       │
│    show_baoming_form_page()                 │
│    - 生成 HTML 表单界面                     │
│    - 用户可以手动修改                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. 提交表单                                 │
│    api.submit_form(form_data)               │
│    - 通过 API 提交                          │
│    - 返回成功/失败结果                      │
└─────────────────────────────────────────────┘
```

### 关键特点
- ✅ **Python 端匹配**（唯一使用 Python 算法的平台）
- ✅ 使用 `SharedMatchAlgorithm.match_keyword()` 共享算法
- ✅ 需要微信扫码登录
- ✅ 通过 API 提交，不是 JavaScript 注入
- ✅ 支持多种字段类型：文本、数字、单选、多选、下拉等

---

## 🍅 番茄表单流程

### 文件位置
`gui/new_fill_window.py::generate_fanqier_fill_script()`

### 域名
- `fanqier.cn`

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. 注入共享算法                             │
│    TencentDocsFiller.get_shared_match_algorithm()
│    TencentDocsFiller.get_shared_execution_logic()
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 解析表单结构                             │
│    parseFanqierFields()                     │
│    - 选择器: [data-type], .fq-field         │
│    - 提取: title, inputType, element        │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. 使用共享执行器                           │
│    createSharedExecutor({                   │
│      getIdentifiers: [field.title],         │
│      fillInput: fillInputFanqier            │
│    })                                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. 平台专用填充适配器                       │
│    fillInputFanqier()                       │
│    - text: fillInput() (Vue 兼容)           │
│    - select: handleSelect() (Element UI)    │
│    - radio/checkbox: handleRadioCheckbox()  │
└─────────────────────────────────────────────┘
```

### 关键特点
- ✅ 使用 `createSharedExecutor` 共享执行器
- ✅ Vue + Element UI 框架深度兼容
- ✅ 支持多种字段类型：text, select, radio, checkbox
- ✅ 兼容模式（fallbackFill）作为后备

---

## 📊 见数流程

### 文件位置
`gui/new_fill_window.py::generate_credamo_fill_script()`

### 域名
- `credamo.com`

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. 注入共享算法                             │
│    TencentDocsFiller.get_shared_match_algorithm()
│    TencentDocsFiller.get_shared_execution_logic()
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 等待输入框加载                           │
│    waitForInputs(20, 500)                   │
│    - Vue/Element-UI 组件加载检测            │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. 获取所有输入框                           │
│    getAllInputs()                           │
│    - 多选择器支持: input, textarea,         │
│      .el-input__inner, .ant-input 等        │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. 使用共享执行器                           │
│    createSharedExecutor({                   │
│      getIdentifiers: 见数专用8种方法提取,   │
│      fillInput: fillInputCredamo            │
│    })                                       │
└─────────────────────────────────────────────┘
```

### 标识符提取（8种方法）
1. `regular-answer` 容器文本
2. 问题容器标题 (`.question-item`, `.el-form-item`)
3. `aria-labelledby` 属性
4. `label` 标签
5. `for` 属性关联
6. 基本属性 (`placeholder`, `name`, `title`)
7. 父元素文本（6层遍历）
8. 前置兄弟元素（3个）

### 关键特点
- ✅ 使用 `createSharedExecutor` 共享执行器
- ✅ Vue/Element-UI/Ant Design 兼容
- ✅ 智能等待输入框加载
- ✅ 多种标识符提取方法

---

## 📝 麦客表单流程

### 文件位置
`core/auto_fill_v2.py::AutoFillEngineV2.generate_fill_script()`

### 域名
- `mikecrm.com` （麦客表单）
- `mike-x.com` （麦客企业版）

### 完整流程

```
┌─────────────────────────────────────────────┐
│ 1. 注入共享算法                             │
│    TencentDocsFiller.get_shared_match_algorithm()
│    TencentDocsFiller.get_shared_execution_logic()
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. 等待输入框加载                           │
│    waitForInputs(5, 300)                    │
│    - 快速检测: 5次 × 300ms                  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. 获取所有输入框                           │
│    getAllInputs()                           │
│    - 选择器: input, textarea               │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. 使用共享执行器                           │
│    createSharedExecutor({                   │
│      getIdentifiers: 麦客专用9种方法提取,   │
│      fillInput: fillInputMike (React)       │
│    })                                       │
└─────────────────────────────────────────────┘
```

### 标识符提取（9种方法 - 麦客CRM专用）
1. `aria-labelledby` 属性（t- 主标题优先）
2. 最小包含容器标签查找
3. `label` 标签
4. `for` 属性关联
5. `aria-label` 属性
6. `placeholder` 属性
7. 父元素标签遍历（5层）
8. 前置兄弟元素（5个）
9. `form-item` 类元素内标签

### 关键特点
- ✅ 使用 `createSharedExecutor` 共享执行器
- ✅ React 深度兼容（Fiber 节点触发）
- ✅ 支持麦客企业版（mike-x.com）
- ✅ 9种标识符提取方法

---

## 🔄 流程对比

| 维度           | 腾讯文档              | WPS                         | 腾讯问卷                    | 石墨文档                    | 金数据                      | 飞书问卷                    | 问卷网                      | 问卷星                      | 报名工具                    | 番茄表单                    | 见数                        | 麦客表单                    |
| -------------- | --------------------- | --------------------------- | --------------------------- | --------------------------- | --------------------------- | --------------------------- | --------------------------- | --------------------------- | --------------------------- | --------------------------- | --------------------------- | --------------------------- |
| **数据格式**   | `{key: value}` 对象   | `[{key, value}]` 数组       | `[{key, value}]` 数组       | `[{key, value}]` 数组       | `[{key, value}]` 数组       | `[{key, value}]` 数组       | `[{key, value}]` 数组       | `[{key, value}]` 数组       | `[{name, value}]` 数组      | `[{key, value}]` 数组       | `[{key, value}]` 数组       | `[{key, value}]` 数组       |
| **等待策略**   | 10次 × 500ms          | 15次 × 400ms                | 20次 × 400ms                | 15次 × 400ms                | 15次 × 400ms                | 25次 × 400ms                | 20次 × 500ms                | 500ms 初始等待              | API 调用                    | 800ms 初始等待              | 20次 × 500ms                | 5次 × 300ms                 |
| **DOM选择器**  | `.question[data-qid]` | `input, textarea`           | `.question`                 | `fieldset`, `input`         | `.field-container`          | `.bitable-form-item`        | `.question-box`             | `.field[type]`              | 无（API）                   | `[data-type], .fq-field`    | `input, .el-input__inner`   | `input, textarea`           |
| **标识符提取** | 简单（问题标题）      | 复杂（6种方法）             | 简单（问题标题）            | 复杂（7种方法）             | 中等（3种方法）             | 简单（字段标题）            | 复杂（7种方法）             | 智能（去序号+括号提取）     | 直接 field_name             | 简单（字段标题）            | 复杂（8种方法）             | 复杂（9种方法）             |
| **特殊处理**   | disabled/readonly     | Radio去重 + 选项过滤        | React/Vue 框架兼容          | React 18+ 数字输入框        | Ant Design 组件             | 新旧版本 + 移动端           | 序号去除 + Element UI       | 多题型 + 兼容模式           | 微信扫码登录                | Vue + Element UI            | Vue + Element UI + Ant      | React Fiber 触发            |
| **填充逻辑**   | textarea 专用         | 通用 + Radio/Checkbox       | InputEvent + 键盘模拟       | valueTracker + Fiber更新    | React + Ant Design          | 富文本编辑器 + 选择器       | Vue + Element UI            | 多题型 + select2            | Python 端渲染               | Vue + Element UI            | Vue + Element UI + Ant      | React 深度兼容              |
| **事件触发**   | input, change, blur   | input, change, blur + React | 完整的事件链 + Vue兼容      | 完整事件 + Composition      | 完整事件 + Composition      | 完整事件 + Composition      | 完整事件 + Composition      | 完整事件 + jqcheck/jqradio  | API 提交                    | 完整事件 + Composition      | 完整事件 + Composition      | React 合成事件              |
| **返回格式**   | filled/failed 数组    | results 数组                | results 数组                | results 数组                | results 数组                | results 数组                | results 数组                | results 数组                | API 响应                    | results 数组                | results 数组                | results 数组                |
| **匹配语言**   | JavaScript            | JavaScript                  | JavaScript                  | JavaScript                  | JavaScript                  | JavaScript                  | JavaScript                  | JavaScript                  | **Python**                  | JavaScript                  | JavaScript                  | JavaScript                  |

---

## ⚙️ 核心共同点

### 1. 匹配算法（100%相同）

```javascript
// 两个平台都使用完全相同的评分系统
matchKeyword(identifiers, keyword)
├─ 100分: 完全匹配
├─ 80-90分: 包含匹配
├─ 70分: 反向包含
└─ 30-60分: 字符相似度
```

### 2. 执行逻辑（100%相同）

```javascript
// 两个平台都使用相同的遍历逻辑
for (const input of allInputs) {
  // 1. 提取标识符（平台特定）
  const identifiers = getIdentifiers(input);

  // 2. 独立查找最高分（共享逻辑）
  let maxScore = 0;
  for (const cardItem of fillData) {
    const result = matchKeyword(identifiers, cardItem.key);
    if (result.score > maxScore) {
      maxScore = result.score;
      matchedValue = cardItem.value;
    }
  }

  // 3. 执行填充（平台特定）
  if (maxScore >= 50) {
    fillInput(input, matchedValue);
  }
}
```

### 3. 关键特性

- ✅ **名片数据可以被多次使用**（不会被"消耗"）
- ✅ **独立匹配**（每个输入框独立查找最高分）
- ✅ **分数阈值**（50分）
- ✅ **延迟填充**（50ms）

---

## 🎯 扩展新平台的步骤

### 只需实现3个平台特定的函数

```javascript
// 1. 等待函数（可选，使用默认的 waitForInputs 也可以）
async function waitForPage() {
    // 等待页面特定元素加载
}

// 2. 标识符提取函数（必须）
function getIdentifiers(input, index) {
    // 根据平台 DOM 结构提取标识符
    return ['标题', '备选标识1', '备选标识2'];
}

// 3. 填充函数（必须）
function fillInput(input, value) {
    // 根据平台特性执行填充
    input.value = value;
    // 触发相应事件
}

// 然后调用共享执行器
const executor = createSharedExecutor({
    fillData: [...],
    allInputs: [...],
    getIdentifiers: getIdentifiers,
    fillInput: fillInput
});

await executor.execute();
```

---

## 📊 性能对比

| 指标         | 腾讯文档         | WPS               |
| ------------ | ---------------- | ----------------- |
| **匹配速度** | 快（简单标识符） | 中（复杂标识符）  |
| **准确率**   | 高（标题清晰）   | 高（多方法兜底）  |
| **资源消耗** | 低               | 中（多次DOM查询） |
| **兼容性**   | 特定于腾讯文档   | 通用性强          |

---

## 🔑 核心优势总结

1. **代码复用**: 匹配算法和执行逻辑统一管理
2. **易于维护**: 修改一处，多个平台生效
3. **逻辑一致**: 两个平台行为完全相同
4. **更好的匹配**: 名片数据可以被多次使用
5. **易于扩展**: 新平台只需3个适配函数

---

## 📝 使用建议

### 调试时

```javascript
// 使用 onProgress 回调查看详细日志
const executor = createSharedExecutor({
  // ...
  onProgress: (msg) => {
    console.log("[DEBUG]", msg);
  },
});
```

### 自定义阈值

```javascript
// 在 matchKeyword 之后手动调整阈值
if (result.score >= 60) {
  // 提高到 60 分
  // 接受匹配
}
```

### 性能优化

```javascript
// 如果输入框很多，可以添加进度反馈
if (i % 10 === 0) {
  console.log(`进度: ${i}/${allInputs.length}`);
}
```
